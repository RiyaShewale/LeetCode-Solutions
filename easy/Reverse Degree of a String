class Solution {
public:
    int reverseDegree(string s) {
        int n=s.length();
        int ans=0;
        for(int i=0;i<n;i++)
        {
            int pro=(i+1)*(26-(s[i]-97));
            ans+=pro;
        }
return ans;
    }
};
